# Supabase via Vercel — estas variáveis são preenchidas automaticamente:
# POSTGRES_PRISMA_URL        (pooler — usado pelo Prisma em runtime)
# POSTGRES_URL_NON_POOLING   (direto — usado para migrations)

# Adicionar manualmente na Vercel:
NEXTAUTH_SECRET="gere-com-openssl-rand-base64-32"
